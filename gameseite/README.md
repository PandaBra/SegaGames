# Sega Genesis Online Emulator

Этот проект представляет собой веб-эмулятор Sega Genesis с возможностью добавления своих игр, чатом и системой отзывов.

## 🚀 Как запустить

Для работы чата и сохранения отзывов требуется Python 3.x.

1. Установите Python (если не установлен): [python.org](https://www.python.org/)
2. Скачайте этот проект.
3. Запустите файл `server.py` двойным кликом (или через терминал `python server.py`).
4. Откройте браузер по адресу: [http://localhost:8080](http://localhost:8080)

## 📱 Игра на телефоне

Чтобы играть с телефона:
1. Подключите телефон к той же Wi-Fi сети, что и компьютер.
2. При запуске сервера в консоли будет написан локальный IP (например, `http://192.168.1.5:8080`).
3. Откройте этот адрес на телефоне.

## 🌐 Публикация в Интернет

Чтобы сайт был доступен всем в интернете (а не только дома), вам понадобится хостинг с поддержкой Python.

### Вариант 1: Render.com (Бесплатно)
1. Создайте репозиторий на GitHub и загрузите туда файлы проекта.
2. Зарегистрируйтесь на [render.com](https://render.com).
3. Создайте "Web Service".
4. Подключите ваш репозиторий GitHub.
5. В настройках укажите:
   - **Runtime**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python server.py`
6. Ваш сайт будет доступен по выданному адресу.

*Примечание: Файлы чата и отзывов на бесплатном тарифе Render могут стираться при перезапуске сервера (т.к. диск эфемерный).*

### Вариант 2: VPS (Виртуальный сервер)
Для полноценной работы (чтобы чат и отзывы сохранялись навсегда) лучше арендовать недорогой VPS (например, Timeweb, Reg.ru, DigitalOcean).
1. Загрузите файлы на сервер.
2. Запустите `python server.py` (лучше использовать `nohup` или `systemd` для работы в фоне).

## 📂 Структура проекта

- `index.html` - Главная страница
- `player.html` - Страница эмулятора
- `chat.html` - Чат
- `feedback.html` - Отзывы
- `server.py` - Серверная часть (API чата, отзывы)
- `roms/` - Папка с играми
- `reviews/` - Папка с отзывами
